/** @type {import('next').NextConfig} */
const nextConfig = {
  // By default Vercel will handle SSR. If you need a static export, uncomment:
  // output: 'export',
  // trailingSlash: true,
};
module.exports = nextConfig;
